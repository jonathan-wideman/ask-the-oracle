import * as React from 'react';
import { Header } from '..';

// interface IProps {
//   name?: string;
// }

// const Home: React.FC<IProps> = (props: IProps) => (
const Home: React.FC = () => (
  <div>
    <Header></Header>
    <p>ask...</p>
  </div>
);

export default Home;